import 'package:apilesson3/models/currency_model.dart';
import 'package:flutter/material.dart';

class CurrencyPage extends StatelessWidget {
  CurrencyModel lst;
  CurrencyPage({Key? key, required this.lst}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
